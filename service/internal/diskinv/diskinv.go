// Package diskinv provides a cross-platform snapshot of currently-mounted
// disk volumes.  The engine uses this on startup to distinguish genuinely
// hot-plugged drives from drives that were already present when OffShoot
// enumerates them.
package diskinv
